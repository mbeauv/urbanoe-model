export type ImageGalleryImageInfoSimple = {

  /** Unique identifier for the image info. */
  +id: number,

  /** Optional label for the image. */
  +label: ?string,

  /** URL to the image */
  +url: ?string,

  /** Time at which the image was created. */
  +createdAt: ?string
};
